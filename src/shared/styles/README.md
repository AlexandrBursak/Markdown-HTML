# Design-token foundation

Global CSS custom properties live in `src/app/globals.css`. Component and widget styles consume them through CSS Modules.

The product palette, spacing scale, type scale, radii, shadows, and motion tokens are intentionally unresolved until an approved design source exists. Do not invent those values in feature code.
