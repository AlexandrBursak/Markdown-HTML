# API sync report — markdown-to-html

API contract generation skipped: `sad.md` declares only `target_surfaces: [web-frontend]`, while the accepted architecture and data model explicitly define a browser-only feature with no backend, public HTTP/RPC interface, CLI, worker event contract, SDK surface, database, or schema change; `spec.md` and `sad.md` were found and complete for this gate, and no `openapi.yaml` or `events.md` is applicable.
