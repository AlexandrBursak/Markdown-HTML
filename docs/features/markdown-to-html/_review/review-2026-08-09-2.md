# Implementation re-review — markdown-to-html

**Date:** 2026-08-09  
**Scope:** full feature diff from `bf3113d` through `f1a4b1b`, with focused remediation review of `bb33d81^..f1a4b1b`  
**Feature size / route:** S / quick  
**Verdict:** PASS

## Verification

- `pnpm typecheck` — pass
- `pnpm lint` — pass
- `pnpm test` — pass (13 files, 67 tests)
- `pnpm test:browser` — pass (25 Chromium tests); unsafe clipboard assertions wait for browser-confirmed completion before payload inspection
- `pnpm build` — pass
- production cold-load profile — pass (30 samples using the built application and `next start`)
- independent final review — `REVIEW_CLEAN: all remediation findings are resolved through f1a4b1b`

## Findings and resolutions

- **[stage-1] Unsafe nested content bypassed recursive sanitization** — `src/entities/conversion/sanitize.ts`; AC-05. **Fixed:** removing an unsafe link now sanitizes its descendants first. Unit and browser regressions cover a nested unsafe image and require two positioned diagnostics.
- **[stage-1] Unsafe fixtures did not cover copied rich HTML** — `tests/browser/converter-security.spec.ts`; AC-05. **Fixed:** every unsafe fixture is checked in preview, displayed HTML, and copied `text/html`, then loaded in an isolated document and rejected if executable elements or disallowed URL schemes remain.
- **[stage-1] Clipboard coverage replaced the browser capability** — `tests/browser/converter-security.spec.ts`; AC-04. **Fixed:** desktop Chromium grants the real clipboard permissions and reads back both browser-written MIME representations. Separate UI tests cover denied and unavailable capability paths.
- **[stage-1] Clipboard-unavailable fallback did not prove complete selection** — `tests/browser/converter-workflow.spec.ts`; AC-04b. **Fixed:** the browser test verifies focus, selection bounds, selected source, and the complete visible HTML value.
- **[stage-1] Storage failures lacked browser-surface evidence** — `tests/browser/converter-workflow.spec.ts`; AC-06. **Fixed:** access denial and quota failure are forced in browser contexts; both retain the current live-tab value across interaction, keep the warning visible, and create no profile record. Adapter tests separately prove restoration from the same runtime-memory instance. Cross-navigation retention is intentionally excluded because ADR-0004 defines fallback as runtime memory rather than a second persistent store.
- **[stage-2] Scheduled performance evidence used the development server** — `package.json` and `playwright.config.ts`. **Fixed:** the full profile builds first and selects `next start`; ordinary browser smoke retains the development server.

## Gate result

`PASS`: every stage-1 finding is fixed, the stage-2 performance concern is fixed, all repository gates pass, and the final clean-context reviewer reported no remaining finding.
